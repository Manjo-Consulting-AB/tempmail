# Client scaffold

This folder contains the first implementation scaffold for the mailfilter client split:

- agent: local processing and webhook handling
- backend: list management, pending sync scheduling and webhook dispatch

The initial version is intentionally simple and file-based so it can be run and tested locally before moving to a database-backed implementation.
