<?php
/**
 * Landing — personal addresses. Spec: documentaion/REDESIGN_BRIEF.md §8.
 *
 * Stub: the section id and the approved heading only. The id is `features`, not
 * `addresses`, because this is where the nav's Features anchor lands — the
 * feature run starts here. Redesign 11 adds the copy and address examples.
 */

if (!defined('TEMPMAIL_APP')) { http_response_code(403); exit; }
?>
<!-- Filled in by Redesign 11 -->
<section class="ms-section" id="features">
    <div class="ms-container">
        <h2 class="ms-h2">One account. Multiple addresses.</h2>
    </div>
</section>
